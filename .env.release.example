# 이 파일을 .env.release 로 복사한 뒤 값을 채우세요. (.env.release 는 git에 올라가지 않습니다)
# 앱 암호(App-Specific Password)는 https://appleid.apple.com 의 "앱 암호"에서 발급합니다.
# pnpm dist:mac / dist:publish 가 자동으로 이 값을 읽어 앱과 DMG를 공증합니다.

APPLE_ID="shape960601@gmail.com"
APPLE_APP_SPECIFIC_PASSWORD="xxxx-xxxx-xxxx-xxxx"
APPLE_TEAM_ID="6W9KP3964C"
